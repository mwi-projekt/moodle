<div class="dhbwio-application-form-page">

    <div class="card mb-3">
        <div class="card-header">
            <h3 class="mb-0">{{title}}</h3>
        </div>

        <div class="card-body">
            <p class="text-muted">
                Bitte füllen Sie die Angaben zur Bewerbung vollständig aus.
            </p>

            {{{formhtml}}}
        </div>
    </div>

    <div class="dhbwio-application-actions">
        <a href="{{backurl}}" class="btn btn-secondary">Zurück zur Übersicht</a>
    </div>

</div>